# JDBC Generated Keys — Interview Questions

---

## Fresher Level

1. What are generated keys in JDBC?
2. Why do we need generated keys?
3. Which method retrieves generated keys?

---

## 1–2 YOE

1. How do you get auto-generated ID after INSERT?
2. What does Statement.RETURN_GENERATED_KEYS do?
3. What does getGeneratedKeys() return?
4. Why must keys.next() be called?

---

## 2–3 YOE

1. Can executeUpdate() return generated ID?
2. When would generated keys be required in backend systems?
3. How would you handle parent-child inserts using JDBC?
4. Are generated keys database dependent?

---
